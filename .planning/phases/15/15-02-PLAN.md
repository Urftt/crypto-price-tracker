---
phase: 15-ui-component-library
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/components/AddHoldingForm.jsx
  - frontend/src/components/AddAlertForm.jsx
  - frontend/src/components/AlertList.jsx
  - frontend/src/components/CoinModal.jsx
  - frontend/src/components/DownloadReport.jsx
  - frontend/src/components/InstallButton.jsx
  - frontend/src/components/PortfolioTable.jsx
  - frontend/src/components/PriceChart.jsx
  - frontend/src/components/PriceTable.jsx
  - frontend/src/components/Toast.jsx
  - frontend/src/pages/WatchlistPage.jsx
  - frontend/src/pages/PricesPage.jsx
  - frontend/src/pages/AlertsPage.jsx
  - frontend/src/App.jsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08]

must_haves:
  truths:
    - "Every <button> element in the codebase is rendered via <Button> from ui/Button.jsx (22 buttons across 10 files)"
    - "Every form text/number/date input is rendered via <Input> from ui/Input.jsx with a visible <label>"
    - "All 3 tables (PriceTable, WatchlistPage, PortfolioTable) use <Table>, <Th>, <Td> from ui/Table.jsx"
    - "CoinModal uses <Modal> wrapper with working Escape-to-close and focus trap"
    - "AlertList direction badges and WatchlistPage tag badges use <Badge> from ui/Badge.jsx"
    - "App.jsx nav uses <NavTab> instead of 4 duplicate NavLink blocks"
    - "All form fields have visible <label> elements (COMP-07)"
    - "AddHoldingForm, AddAlertForm, and WatchlistPage add form disable submit button during API call (COMP-08)"
    - "No duplicate inputClass string or inline button style patterns remain in the codebase"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "NavTab usage replacing 4 NavLink duplicates"
      contains: "NavTab"
    - path: "frontend/src/components/CoinModal.jsx"
      provides: "Modal wrapper with Escape/focus-trap"
      contains: "import { Modal }"
    - path: "frontend/src/components/AddHoldingForm.jsx"
      provides: "Input with labels + Button with loading state"
      contains: "submitting"
    - path: "frontend/src/components/AddAlertForm.jsx"
      provides: "Input with labels + Button with loading state"
      contains: "submitting"
    - path: "frontend/src/pages/WatchlistPage.jsx"
      provides: "Badge, Table, Th, Td, Button, Input usage"
      contains: "import { Badge }"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "frontend/src/components/ui/NavTab.jsx"
      via: "import NavTab, uses <NavTab to='/' end>"
      pattern: "import.*NavTab.*from.*ui/NavTab"
    - from: "frontend/src/components/CoinModal.jsx"
      to: "frontend/src/components/ui/Modal.jsx"
      via: "import Modal, wraps content in <Modal open onClose>"
      pattern: "import.*Modal.*from.*ui/Modal"
    - from: "frontend/src/pages/PricesPage.jsx"
      to: "frontend/src/components/CoinModal.jsx"
      via: "Always renders CoinModal with open prop instead of conditional"
      pattern: "<CoinModal.*open="
    - from: "frontend/src/components/AddHoldingForm.jsx"
      to: "frontend/src/components/ui/Button.jsx"
      via: "Button loading={submitting} for double-submit prevention"
      pattern: "loading=.submitting"
---

<objective>
Refactor all existing components to use the new UI primitives, add visible labels to all form fields, and add loading/disabled state during form submission.

Purpose: This is the integration step -- swapping every inline button, input, table, modal, badge, and nav link with the shared primitives from Plan 01. After this, zero duplicate style strings remain and all forms are accessible.

Output: All 14 modified files using shared primitives, all existing + new tests passing, no inputClass or inline button styles remaining.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15/15-CONTEXT.md
@.planning/phases/15/15-RESEARCH.md
@.planning/phases/15/15-01-SUMMARY.md

<interfaces>
<!-- UI primitives created in Plan 15-01. Use exactly these imports. -->

From frontend/src/components/ui/Button.jsx:
```jsx
// Props: variant ('primary'|'secondary'|'danger'|'ghost'), size ('xs'|'sm'|'md'), loading, className, children, disabled, ...props
// NO default type -- passes through from props
export function Button({ variant = 'primary', size = 'md', loading = false, className = '', children, disabled, ...props })
```

From frontend/src/components/ui/Input.jsx:
```jsx
// Props: label, error, className, id, ...props
// Auto-generates id from label. Renders <label> + <input> + error <p>.
// Width controlled via className on outer div. Inner input is w-full.
export function Input({ label, error, className = '', id, ...props })
```

From frontend/src/components/ui/Table.jsx:
```jsx
// Table: just a <table> with w-full
export function Table({ className, ...props })
// Th: align prop ('left'|'right'|'center'), uses explicit alignMap
export function Th({ align = 'left', className = '', ...props })
// Td: align prop ('left'|'right'|'center'), uses explicit alignMap
export function Td({ align = 'left', className = '', ...props })
```

From frontend/src/components/ui/Modal.jsx:
```jsx
// Props: open (boolean), onClose (function), children
// Returns null when !open. Has Escape, click-outside, focus trap.
export function Modal({ open, onClose, children })
```

From frontend/src/components/ui/Badge.jsx:
```jsx
// Props: colorScheme ('blue'|'purple'|'green'|'yellow'|'orange'|'red'|'gray'), variant ('up'|'down'), className, children, ...props
export function Badge({ colorScheme, variant, className = '', children, ...props })
```

From frontend/src/components/ui/NavTab.jsx:
```jsx
// Props: to (string), end (boolean), children
export function NavTab({ to, end, children })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor all components to use UI primitives and add labels</name>
  <files>
    frontend/src/App.jsx
    frontend/src/components/CoinModal.jsx
    frontend/src/pages/PricesPage.jsx
    frontend/src/components/PriceTable.jsx
    frontend/src/components/PriceChart.jsx
    frontend/src/components/Toast.jsx
    frontend/src/components/DownloadReport.jsx
    frontend/src/components/InstallButton.jsx
    frontend/src/components/AlertList.jsx
    frontend/src/pages/AlertsPage.jsx
    frontend/src/components/PortfolioTable.jsx
    frontend/src/pages/WatchlistPage.jsx
    frontend/src/components/AddHoldingForm.jsx
    frontend/src/components/AddAlertForm.jsx
  </files>
  <action>
Refactor each file in order of complexity (simplest first). For EVERY change, the visual output MUST remain pixel-identical -- only the JSX structure changes, not the rendered classes.

**File 1: `frontend/src/App.jsx`** (COMP-06)

Replace the NavLink import with NavTab import:
```jsx
import { NavTab } from './components/ui/NavTab';
```
Remove `NavLink` from the `react-router` import (keep `Routes`, `Route`).

Replace lines 25-28 (the 4 NavLink blocks) with:
```jsx
<NavTab to="/" end>Prices</NavTab>
<NavTab to="/watchlist">Watchlist</NavTab>
<NavTab to="/portfolio">Portfolio</NavTab>
<NavTab to="/alerts">Alerts</NavTab>
```

CRITICAL: The first NavTab MUST have `end` prop to prevent "/" from matching all routes.

**File 2: `frontend/src/components/DownloadReport.jsx`** (COMP-01)

Add import: `import { Button } from './ui/Button';`

Replace the `<button>` on line 25-31 with:
```jsx
<Button
  onClick={handleDownload}
  disabled={loading}
  variant="secondary"
  size="md"
>
  {loading ? 'Generating...' : 'Download Report'}
</Button>
```
Note: DownloadReport already has its own loading state; do NOT add a second one. The existing `disabled={loading}` and custom text handling stays. Do NOT use Button's `loading` prop here because the text says "Generating..." not "Submitting...".

**File 3: `frontend/src/components/InstallButton.jsx`** (COMP-01)

Add import: `import { Button } from './ui/Button';`

Replace the `<button>` on line 9-12 with:
```jsx
<Button
  onClick={install}
  variant="ghost"
  size="md"
  className="bg-accent/20 border border-accent/40 text-accent hover:bg-accent/30"
>
  Install App
</Button>
```
Note: InstallButton has a unique style (accent ghost). Use variant="ghost" as base and override with className for the specific accent styling. This preserves the exact visual appearance.

**File 4: `frontend/src/components/Toast.jsx`** (COMP-01)

Add import: `import { Button } from './ui/Button';`

Replace the close `<button>` on line 21 with:
```jsx
<Button variant="ghost" size="xs" onClick={onClose} type="button" className="ml-3">
  x
</Button>
```

**File 5: `frontend/src/components/PriceChart.jsx`** (COMP-01)

Add import: `import { Button } from './ui/Button';`

Replace the two period toggle buttons (lines 38-57). For each:
- Active state: `<Button variant="primary" size="sm" type="button" onClick={...}>7D</Button>`
- Inactive state: `<Button variant="ghost" size="sm" type="button" className="bg-border" onClick={...}>30D</Button>`

Use a ternary: `period === '7d' ? variant="primary" : variant="ghost" className="bg-border"` for the 7D button, and inverse for 30D.

IMPORTANT: Both buttons MUST have `type="button"` since they are not in a form but we don't want any accidental submit behavior.

**File 6: `frontend/src/pages/PricesPage.jsx`** (COMP-01, COMP-04)

Add import: `import { Button } from '../components/ui/Button';`

Replace the Refresh button (line 63-66) with:
```jsx
<Button variant="secondary" size="sm" onClick={handleRefresh} type="button">
  Refresh
</Button>
```
Note: The existing Refresh button has `hover:border-accent hover:text-accent` which is extra. Use variant="secondary" size="sm" which gives `bg-card border border-border text-text hover:border-border-light px-2.5 py-0.5 text-xs`. To match the original exactly, add `className="bg-border border-border-light hover:border-accent hover:text-accent"` override.

Change CoinModal rendering: Instead of conditional `{selectedCoin && <CoinModal ... />}`, always render CoinModal and pass `open` prop:
```jsx
<CoinModal coin={selectedCoin} open={!!selectedCoin} onClose={() => setSelectedCoin(null)} />
```

**File 7: `frontend/src/components/CoinModal.jsx`** (COMP-04, COMP-01)

Add imports:
```jsx
import { Modal } from './ui/Modal';
import { Button } from './ui/Button';
```

Replace the entire outer div structure. The component currently renders its own backdrop + content div. Replace with `<Modal open={open} onClose={onClose}>` wrapper.

Add `open` to the component props: `function CoinModal({ coin, open, onClose })`.

New structure:
```jsx
function CoinModal({ coin, open, onClose }) {
  const navigate = useNavigate();

  const handleSetAlert = () => {
    navigate(`/alerts?symbol=${coin?.symbol}`);
    onClose();
  };

  return (
    <Modal open={open} onClose={onClose}>
      <Button variant="ghost" size="sm" onClick={onClose} type="button" className="absolute top-3 right-4 text-xl">
        x
      </Button>
      {coin && (
        <>
          {/* existing content unchanged: coin header, grid, chart */}
          <div className="mb-4">
            <span className="text-accent text-2xl font-bold mr-2">{coin.symbol}</span>
            <span className="text-text-muted">{coin.name}</span>
          </div>
          {/* ... rest of content stays identical ... */}
          <div className="mt-4 flex justify-end">
            <Button
              onClick={handleSetAlert}
              variant="secondary"
              size="md"
              type="button"
              className="bg-accent/20 text-accent border-accent/30 hover:bg-accent/30"
            >
              Set Alert
            </Button>
          </div>
        </>
      )}
    </Modal>
  );
}
```

Keep all the existing content between the close button and the Set Alert button exactly as-is (grid with price/change/volume/etc, PriceChart component).

**File 8: `frontend/src/components/PriceTable.jsx`** (COMP-01, COMP-03)

Add imports:
```jsx
import { Table, Th, Td } from './ui/Table';
import { Button } from './ui/Button';
```

Replace `<table className="w-full max-w-4xl">` with `<Table className="max-w-4xl">`.

Replace all 7 `<th>` elements (lines 45-51) with `<Th>` components:
```jsx
<Th align="center" className="w-8 px-1"></Th>
<Th>#</Th>
<Th>Symbol</Th>
<Th>Name</Th>
<Th align="right">Price (EUR)</Th>
<Th align="right">24h %</Th>
<Th align="right">Volume (EUR)</Th>
```
Note: The star column header uses `px-1` and `w-8` overrides via className.

Replace all `<td>` elements with `<Td>`:
- Star cell: `<Td align="center" className="px-1">` (keeps the reduced padding)
- Index cell: `<Td className="text-text-dim text-sm">`
- Symbol cell: `<Td className="text-accent font-bold">`
- Name cell: `<Td className="text-text-muted text-sm">`
- Price cell: `<Td align="right">`
- Change cell: `<Td align="right" className={...color class...}>`
- Volume cell: `<Td align="right" className="text-text-muted">`

Replace the star toggle `<button>` with:
```jsx
<Button
  variant="ghost"
  size="xs"
  onClick={(e) => toggleWatchlist(e, coin.symbol)}
  type="button"
  className={`text-sm ${isWatched ? 'text-yellow-400' : 'text-text-dim hover:text-yellow-400/60'}`}
  title={isWatched ? 'Remove from watchlist' : 'Add to watchlist'}
>
  {isWatched ? '\u2605' : '\u2606'}
</Button>
```

**File 9: `frontend/src/components/AlertList.jsx`** (COMP-01, COMP-05)

Add imports:
```jsx
import { Button } from './ui/Button';
import { Badge } from './ui/Badge';
```

Replace the direction badge `<span>` (lines 18-26) with:
```jsx
<Badge variant={alert.direction === 'above' ? 'up' : 'down'}>
  {alert.direction}
</Badge>
```

Replace the Remove `<button>` (lines 38-43) with:
```jsx
<Button variant="danger" size="sm" onClick={() => onRemove(alert.id)} type="button">
  Remove
</Button>
```

**File 10: `frontend/src/pages/AlertsPage.jsx`** (COMP-01)

Add import: `import { Button } from '../components/ui/Button';`

Replace the Clear All `<button>` (lines 74-79) with:
```jsx
<Button variant="danger" size="sm" onClick={handleClearTriggered} type="button">
  Clear All
</Button>
```

**File 11: `frontend/src/components/PortfolioTable.jsx`** (COMP-01, COMP-02, COMP-03)

Add imports:
```jsx
import { Table, Th, Td } from './ui/Table';
import { Button } from './ui/Button';
import { Input } from './ui/Input';
```

Replace `<table className="w-full">` with `<Table>`.

Replace all 8 `<th>` elements (lines 75-82) with `<Th>`:
```jsx
<Th>Symbol</Th>
<Th align="right">Amount</Th>
<Th align="right">Avg Buy</Th>
<Th align="right">Current</Th>
<Th align="right">Value</Th>
<Th align="right">P&L EUR</Th>
<Th align="right">P&L %</Th>
<Th align="right">Alloc %</Th>
```

Replace all `<td>` elements with `<Td>`:
- Summary row: 8 Td components matching existing alignment (first is left, rest are right)
- Lot rows: Td for lot#, amount, buy_price, buy_date, empty colspan, actions

For the inline edit input (line 116-124), replace with Input using aria-label (NOT visible label per user decision):
```jsx
<Input
  type="number"
  step="any"
  value={editAmount}
  onChange={(e) => setEditAmount(e.target.value)}
  onKeyDown={(e) => { if (e.key === 'Enter') handleEditLot(lot.id); if (e.key === 'Escape') { setEditingLotId(null); setEditAmount(''); } }}
  onClick={(e) => e.stopPropagation()}
  aria-label="Edit amount"
  className="w-20"
  autoFocus
/>
```
Note: No `label` prop -- uses `aria-label` instead per CONTEXT.md decision. The className="w-20" controls width on the outer div; the inner input is w-full.

NOTE: The inline edit input has smaller sizing (text-xs, px-1 py-0.5) compared to the standard Input. To match this, add a `className` override on Input: `className="w-20 [&_input]:text-xs [&_input]:px-1 [&_input]:py-0.5"`. Alternatively, keep the raw `<input>` for this one special case since it is fundamentally different from form inputs. Use your judgment -- if the Tailwind child selector approach is too fragile, keep the raw input with just an added `aria-label="Edit amount"` attribute.

Replace all lot action buttons with Button:
- Save: `<Button variant="ghost" size="sm" className="text-up hover:text-up/80" onClick={...} type="button">Save</Button>`
- Cancel: `<Button variant="ghost" size="sm" onClick={...} type="button">Cancel</Button>`
- Edit: `<Button variant="ghost" size="sm" className="text-accent hover:text-accent/80" onClick={...} type="button">Edit</Button>`
- Delete: `<Button variant="danger" size="sm" onClick={...} type="button">Delete</Button>`

All lot action buttons MUST have `onClick={(e) => { e.stopPropagation(); ...action... }}` to prevent triggering the parent row's toggleLots click handler.

**File 12: `frontend/src/pages/WatchlistPage.jsx`** (COMP-01, COMP-02, COMP-03, COMP-05, COMP-07)

Add imports:
```jsx
import { Table, Th, Td } from '../components/ui/Table';
import { Button } from '../components/ui/Button';
import { Input } from '../components/ui/Input';
import { Badge } from '../components/ui/Badge';
```

Remove the `inputClass` variable (line 119).

Create a `TAG_COLOR_MAP` that maps tag names to Badge colorScheme values:
```jsx
const TAG_COLOR_MAP = {
  Layer1: 'blue',
  Layer2: 'purple',
  DeFi: 'green',
  Meme: 'yellow',
  Exchange: 'orange',
  Privacy: 'red',
};
```
Keep the existing `TAG_COLORS` object (still needed for the tag toggle buttons which have custom opacity/ring styles that Badge doesn't handle). OR replace tag display `<span>` elements with `<Badge>` and keep toggle buttons as-is.

Replace the add form input (line 132-138) with:
```jsx
<Input label="Symbol" type="text" placeholder="BTC" value={symbol}
  onChange={(e) => setSymbol(e.target.value)} className="w-24"
  style={{ textTransform: 'uppercase' }} required />
```
Note: `uppercase` was previously on the input as a Tailwind class. Since Input wraps in a div, use `style={{ textTransform: 'uppercase' }}` which passes through to the input via `...props`. Alternatively use `className="w-24 uppercase"` on the outer div -- but uppercase on a div affects all text including the label. Better: add `inputClassName` support OR just keep `style` passthrough.

Actually, the simplest approach: keep the Input component as-is and pass `style={{ textTransform: 'uppercase' }}` which flows to the `<input>` via `...props`.

Replace the submit button (line 154-159) with:
```jsx
<Button type="submit" variant="primary" size="md">Add to Watchlist</Button>
```

Replace tag display spans (line 247-254) with Badge:
```jsx
<Badge colorScheme={TAG_COLOR_MAP[tag] || 'gray'} className="px-1.5 py-0.5">
  {tag}
</Badge>
```

Replace the table (line 198-292):
- `<table>` -> `<Table className="max-w-4xl">`
- All 7 `<th>` -> `<Th>` with correct alignment
- All `<td>` -> `<Td>` with correct alignment
- Edit Tags button -> `<Button variant="ghost" size="sm" className="text-accent hover:text-accent/80" type="button">`
- Remove button -> `<Button variant="danger" size="sm" type="button">`
- Save button -> `<Button variant="ghost" size="sm" className="text-up hover:text-up/80" type="button">`
- Cancel button -> `<Button variant="ghost" size="sm" type="button">`
- Clear filter button -> `<Button variant="ghost" size="sm" type="button">`

**File 13: `frontend/src/components/AddHoldingForm.jsx`** (COMP-01, COMP-02, COMP-07, COMP-08)

Add imports:
```jsx
import { Button } from './ui/Button';
import { Input } from './ui/Input';
```

Remove the `inputClass` variable (line 32).

Add `submitting` state:
```jsx
const [submitting, setSubmitting] = useState(false);
```

Wrap the submit handler:
```jsx
const handleSubmit = async (e) => {
  e.preventDefault();
  setError(null);
  setSubmitting(true);
  try {
    await api.post('/api/portfolio', { ... });
    // reset fields...
    onAdded();
  } catch (err) {
    setError(err.message);
  } finally {
    setSubmitting(false);
  }
};
```

Replace the 4 inputs (lines 37-70) with Input components:
```jsx
<Input label="Symbol" type="text" placeholder="BTC" value={symbol}
  onChange={(e) => setSymbol(e.target.value)} className="w-24"
  style={{ textTransform: 'uppercase' }} required />
<Input label="Amount" type="number" placeholder="0.5" value={amount}
  onChange={(e) => setAmount(e.target.value)} className="w-28"
  step="any" min="0" required />
<Input label="Buy Price EUR" type="number" placeholder="50000" value={buyPrice}
  onChange={(e) => setBuyPrice(e.target.value)} className="w-32"
  step="any" min="0" required />
<Input label="Buy Date" type="date" value={buyDate}
  onChange={(e) => setBuyDate(e.target.value)} className="w-36" />
```

Replace the submit button (line 71-76) with:
```jsx
<Button type="submit" loading={submitting}>
  Add Holding
</Button>
```

**File 14: `frontend/src/components/AddAlertForm.jsx`** (COMP-01, COMP-02, COMP-07, COMP-08)

Add imports:
```jsx
import { Button } from './ui/Button';
import { Input } from './ui/Input';
```

Remove the `inputClass` variable (line 35).

Add `submitting` state and wrap handler (same pattern as AddHoldingForm).

Replace the 2 inputs (lines 40-56) with Input components:
```jsx
<Input label="Symbol" type="text" placeholder="BTC" value={symbol}
  onChange={(e) => setSymbol(e.target.value)} className="w-24"
  style={{ textTransform: 'uppercase' }} required />
<Input label="Target Price EUR" type="number" placeholder="50000" value={targetPrice}
  onChange={(e) => setTargetPrice(e.target.value)} className="w-36"
  step="any" min="0" required />
```

Keep the `<select>` as-is (per CONTEXT.md decision: selects keep their existing patterns). But apply the inputClass styling inline since we removed the variable:
```jsx
<select value={direction} onChange={(e) => setDirection(e.target.value)}
  className="bg-bg border border-border rounded px-3 py-1.5 text-text text-sm focus:border-accent focus:outline-none">
```
Add a visible label above the select:
```jsx
<div>
  <label htmlFor="alert-direction" className="text-text-muted text-xs mb-0.5 block">Direction</label>
  <select id="alert-direction" value={direction} onChange={(e) => setDirection(e.target.value)}
    className="bg-bg border border-border rounded px-3 py-1.5 text-text text-sm focus:border-accent focus:outline-none">
    <option value="above">above</option>
    <option value="below">below</option>
  </select>
</div>
```

Replace the submit button with:
```jsx
<Button type="submit" loading={submitting}>
  Add Alert
</Button>
```

**ALSO for WatchlistPage add form (COMP-08):** Add `submitting` state and loading on the submit button, same pattern. Wrap `handleAdd` with `setSubmitting(true)` / `finally { setSubmitting(false) }`.
  </action>
  <verify>
    <automated>cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && npx vitest run --reporter=verbose 2>&1 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 14 files refactored to use UI primitives
    - Zero inline button style strings remain (no `bg-accent text-bg font-bold` etc. outside ui/ components)
    - Zero `inputClass` variable declarations remain
    - All form inputs have visible labels (via Input component's label prop)
    - AddAlertForm select has a visible label element
    - PortfolioTable inline edit has aria-label (no visible label per decision)
    - ExchangeDropdown is unchanged (select, out of scope per decision)
    - AddHoldingForm, AddAlertForm, WatchlistPage add form have submitting state + Button loading
    - All existing vitest tests still pass
    - Frontend builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify zero duplication remains and run full validation</name>
  <files>
    (No new files -- validation only)
  </files>
  <action>
Run a comprehensive audit to confirm all refactoring is complete:

**Check 1: No duplicate inputClass strings**
```bash
cd /home/james-turing/repos/crypto-price-tracker-v2/frontend
grep -rn "inputClass" src/ --include="*.jsx"
```
Expected: zero results (all inputClass variables removed).

**Check 2: No inline button style patterns outside ui/**
```bash
grep -rn "bg-accent text-bg font-bold" src/ --include="*.jsx" | grep -v "ui/"
grep -rn "text-down hover:text-down/80.*cursor-pointer" src/ --include="*.jsx" | grep -v "ui/"
grep -rn "text-text-muted hover:text-text.*cursor-pointer" src/ --include="*.jsx" | grep -v "ui/"
```
Expected: zero results for each. All button styles should live only in ui/Button.jsx.

**Check 3: All forms have labels**
```bash
grep -rn "<label" src/ --include="*.jsx"
grep -rn "aria-label" src/ --include="*.jsx"
```
Expected: label elements in Input.jsx + AddAlertForm.jsx (for select), aria-label in PortfolioTable.jsx.

**Check 4: Loading state on forms**
```bash
grep -rn "submitting" src/ --include="*.jsx"
```
Expected: AddHoldingForm.jsx, AddAlertForm.jsx, WatchlistPage.jsx all have submitting state.

**Check 5: NavTab used in App.jsx**
```bash
grep -n "NavLink" src/App.jsx
grep -n "NavTab" src/App.jsx
```
Expected: No direct NavLink in App.jsx nav (only in imports if needed for Routes), NavTab imported and used.

**Check 6: Modal used in CoinModal**
```bash
grep -n "import.*Modal.*from.*ui/Modal" src/components/CoinModal.jsx
```
Expected: one import line.

**Check 7: Run all frontend tests**
```bash
cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && npx vitest run --reporter=verbose
```
Expected: All tests pass.

**Check 8: Build succeeds**
```bash
cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && npx vite build
```
Expected: Build succeeds with no errors.

**Check 9: Verify backend tests unaffected**
```bash
cd /home/james-turing/repos/crypto-price-tracker-v2 && python -m pytest tests/ -q --tb=no 2>&1 | tail -5
```
Expected: 271 tests passed, 0 failed.

If any check fails, fix the issue before completing. Common fixups:
- Missing import: add the import line
- Leftover inline style: replace with component usage
- Test failure: check that component props match test expectations
- Build error: check for JSX syntax issues or missing exports
  </action>
  <verify>
    <automated>cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && npx vitest run --reporter=verbose 2>&1 && npx vite build 2>&1 | tail -3 && cd /home/james-turing/repos/crypto-price-tracker-v2 && python -m pytest tests/ -q --tb=no 2>&1 | tail -3</automated>
  </verify>
  <done>
    - grep for "inputClass" in src/ returns 0 results
    - grep for inline button patterns outside ui/ returns 0 results
    - All form fields have label or aria-label
    - 3 forms have submitting state (AddHoldingForm, AddAlertForm, WatchlistPage)
    - App.jsx uses NavTab (no direct NavLink in nav)
    - CoinModal uses Modal wrapper
    - All frontend tests pass
    - Frontend build succeeds
    - All 271 backend tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# All frontend tests pass
cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && npx vitest run --reporter=verbose

# Frontend builds without errors
cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && npx vite build

# Backend tests unaffected
cd /home/james-turing/repos/crypto-price-tracker-v2 && python -m pytest tests/ -q --tb=no

# No duplicate styles remain
cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && grep -rn "inputClass" src/ --include="*.jsx" | wc -l
# Expected: 0

# All form fields have labels
cd /home/james-turing/repos/crypto-price-tracker-v2/frontend && grep -rn "<label\|aria-label" src/ --include="*.jsx" | wc -l
# Expected: >= 6 (Input.jsx label, AddAlertForm select label, PortfolioTable aria-label)
```
</verification>

<success_criteria>
- All 22 inline button elements replaced with Button component (COMP-01)
- All inline input styles replaced with Input component (COMP-02)
- All 3 tables use Table/Th/Td (COMP-03)
- CoinModal uses Modal with Escape/click-outside/focus-trap (COMP-04)
- Alert badges and tag display use Badge (COMP-05)
- App.jsx uses NavTab for all 4 nav links (COMP-06)
- All form fields have visible labels or aria-label (COMP-07)
- 3 forms have loading/disabled state during submission (COMP-08)
- Zero inputClass strings remain in codebase
- Zero inline button style patterns remain outside ui/
- All frontend tests pass
- Frontend builds successfully
- All 271 backend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15/15-02-SUMMARY.md`
</output>
