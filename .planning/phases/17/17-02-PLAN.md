---
phase: 17
plan: 2
milestone: v2.0
subsystem: frontend
tags: [skeletons, empty-states, loading, ux]
requirements: [MOB-05, MOB-06]
depends_on: [17-01]
estimated_tasks: 2
---

# Phase 17 Plan 02: Loading Skeletons & Empty States

## Goal

Replace plain text loading indicators and empty messages with responsive skeleton placeholders and styled empty state components, so users see layout-matching visual feedback during data fetches and clear, helpful messages when views have no data.

## Tasks

### Task 1: Create Skeleton and EmptyState UI Primitives with Tests

**Requirements:** MOB-05, MOB-06 (primitives)

#### Changes

**Create `frontend/src/components/ui/Skeleton.jsx`:**

```jsx
export function Skeleton({ className = '', ...props }) {
  return (
    <div
      className={`animate-pulse bg-border rounded ${className}`}
      {...props}
    />
  );
}
```

A minimal div wrapper that accepts `className` for width/height sizing (e.g. `className="h-4 w-16"`) and spreads any additional HTML div props. Uses Tailwind `animate-pulse` for the shimmer effect, `bg-border` to match the app's border color token, and `rounded` for consistent corner rounding.

**Create `frontend/src/components/ui/__tests__/Skeleton.test.jsx`:**

```jsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { Skeleton } from '../Skeleton';

describe('Skeleton', () => {
  it('renders a div with animate-pulse class', () => {
    render(<Skeleton data-testid="skel" />);
    const el = screen.getByTestId('skel');
    expect(el.className).toContain('animate-pulse');
    expect(el.className).toContain('bg-border');
    expect(el.className).toContain('rounded');
  });

  it('appends custom className', () => {
    render(<Skeleton className="h-4 w-16" data-testid="skel" />);
    const el = screen.getByTestId('skel');
    expect(el.className).toContain('h-4');
    expect(el.className).toContain('w-16');
    expect(el.className).toContain('animate-pulse');
  });

  it('passes through additional props', () => {
    render(<Skeleton data-testid="skel" aria-hidden="true" />);
    const el = screen.getByTestId('skel');
    expect(el).toHaveAttribute('aria-hidden', 'true');
  });
});
```

**Create `frontend/src/components/ui/EmptyState.jsx`:**

```jsx
export function EmptyState({ title, description, className = '' }) {
  return (
    <div className={`text-center py-8 ${className}`}>
      <p className="text-text-muted text-lg mb-1">{title}</p>
      {description && (
        <p className="text-text-dim text-sm">{description}</p>
      )}
    </div>
  );
}
```

Centered text block with `py-8` vertical padding. Title renders in `text-text-muted text-lg`, description (optional) renders below in `text-text-dim text-sm`.

**Create `frontend/src/components/ui/__tests__/EmptyState.test.jsx`:**

```jsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { EmptyState } from '../EmptyState';

describe('EmptyState', () => {
  it('renders the title', () => {
    render(<EmptyState title="No items" />);
    expect(screen.getByText('No items')).toBeInTheDocument();
  });

  it('renders description when provided', () => {
    render(<EmptyState title="Empty" description="Add items to get started." />);
    expect(screen.getByText('Add items to get started.')).toBeInTheDocument();
  });

  it('does not render description paragraph when omitted', () => {
    const { container } = render(<EmptyState title="Empty" />);
    const paragraphs = container.querySelectorAll('p');
    expect(paragraphs).toHaveLength(1);
  });

  it('applies custom className to wrapper', () => {
    const { container } = render(<EmptyState title="Empty" className="max-w-4xl" />);
    expect(container.firstChild.className).toContain('max-w-4xl');
    expect(container.firstChild.className).toContain('text-center');
    expect(container.firstChild.className).toContain('py-8');
  });
});
```

#### Verification

```bash
cd frontend && npx vitest run src/components/ui/__tests__/Skeleton.test.jsx src/components/ui/__tests__/EmptyState.test.jsx --reporter=verbose
```

All 7 tests pass. Both files export named components. No build errors:

```bash
cd frontend && npx vite build
```

---

### Task 2: Integrate Skeletons and Empty States Across All Pages

**Requirements:** MOB-05, MOB-06 (integration)

#### Changes

**Modify `frontend/src/pages/PricesPage.jsx`:**

Import `Skeleton` from `../components/ui/Skeleton`. Import `Table`, `Th`, `Td` from `../components/ui/Table`.

Replace the conditional rendering `{prices && <ErrorBoundary>...` block. When `!prices` (before first SSE event arrives), render skeleton loading state. Keep the existing `{prices && ...}` rendering after.

Add this skeleton block before the existing prices conditional (at approximately line 71, replacing the single-line conditional):

```jsx
{!prices && (
  <>
    {/* Mobile card skeletons */}
    <div className="sm:hidden space-y-2">
      {[...Array(5)].map((_, i) => (
        <div key={i} className="bg-card border border-border rounded p-3">
          <div className="flex justify-between mb-2">
            <Skeleton className="h-4 w-16" />
            <Skeleton className="h-4 w-20" />
          </div>
          <div className="flex justify-between">
            <Skeleton className="h-5 w-24" />
            <Skeleton className="h-4 w-12" />
          </div>
        </div>
      ))}
    </div>
    {/* Desktop table skeletons */}
    <div className="hidden sm:block overflow-x-auto">
      <Table className="max-w-4xl">
        <thead>
          <tr>
            <Th className="w-8 px-1"></Th>
            <Th>#</Th>
            <Th>Symbol</Th>
            <Th>Name</Th>
            <Th align="right">Price (EUR)</Th>
            <Th align="right">24h %</Th>
            <Th align="right">Volume (EUR)</Th>
          </tr>
        </thead>
        <tbody>
          {[...Array(5)].map((_, i) => (
            <tr key={i} className="border-b border-border/50">
              <Td className="px-1"><Skeleton className="h-4 w-4" /></Td>
              <Td><Skeleton className="h-4 w-4" /></Td>
              <Td><Skeleton className="h-4 w-12" /></Td>
              <Td><Skeleton className="h-4 w-20" /></Td>
              <Td align="right"><Skeleton className="h-4 w-16" /></Td>
              <Td align="right"><Skeleton className="h-4 w-12" /></Td>
              <Td align="right"><Skeleton className="h-4 w-16" /></Td>
            </tr>
          ))}
        </tbody>
      </Table>
    </div>
  </>
)}
{prices && <ErrorBoundary><PriceTable coins={prices.coins} onSelectCoin={setSelectedCoin} /></ErrorBoundary>}
```

The desktop skeleton table headers match PriceTable's exact columns (star, #, Symbol, Name, Price, 24h%, Volume). Mobile card skeletons use 2-row layout matching the card design from 17-01.

---

**Modify `frontend/src/pages/PortfolioPage.jsx`:**

Import `Skeleton` from `../components/ui/Skeleton`. Import `Table`, `Th`, `Td` from `../components/ui/Table`.

Replace the early return `if (loading) { return <p>Loading portfolio...</p>; }` (lines 27-29) with a skeleton block. Keep the early return pattern but render skeletons instead of text:

```jsx
if (loading) {
  return (
    <div>
      {/* Mobile card skeletons */}
      <div className="sm:hidden space-y-2">
        {[...Array(5)].map((_, i) => (
          <div key={i} className="bg-card border border-border rounded p-3">
            <div className="flex justify-between mb-2">
              <Skeleton className="h-4 w-12" />
              <Skeleton className="h-4 w-16" />
            </div>
            <div className="flex justify-between">
              <Skeleton className="h-5 w-20" />
              <Skeleton className="h-4 w-14" />
            </div>
          </div>
        ))}
      </div>
      {/* Desktop table skeletons */}
      <div className="hidden sm:block max-w-4xl overflow-x-auto">
        <Table>
          <thead>
            <tr>
              <Th>Symbol</Th>
              <Th align="right">Amount</Th>
              <Th align="right">Avg Buy</Th>
              <Th align="right">Current</Th>
              <Th align="right">Value</Th>
              <Th align="right">P&L EUR</Th>
              <Th align="right">P&L %</Th>
              <Th align="right">Alloc %</Th>
            </tr>
          </thead>
          <tbody>
            {[...Array(5)].map((_, i) => (
              <tr key={i} className="border-b border-border/50">
                <Td><Skeleton className="h-4 w-12" /></Td>
                <Td align="right"><Skeleton className="h-4 w-10" /></Td>
                <Td align="right"><Skeleton className="h-4 w-16" /></Td>
                <Td align="right"><Skeleton className="h-4 w-16" /></Td>
                <Td align="right"><Skeleton className="h-4 w-16" /></Td>
                <Td align="right"><Skeleton className="h-4 w-14" /></Td>
                <Td align="right"><Skeleton className="h-4 w-10" /></Td>
                <Td align="right"><Skeleton className="h-4 w-10" /></Td>
              </tr>
            ))}
          </tbody>
        </Table>
      </div>
    </div>
  );
}
```

Desktop skeleton headers match PortfolioTable's 8 columns exactly. Mobile card skeletons match the card layout from 17-01.

---

**Modify `frontend/src/components/PortfolioTable.jsx`:**

Import `EmptyState` from `./ui/EmptyState`.

Replace the empty state text (line 66-68):

```jsx
// BEFORE:
if (!rows || rows.length === 0) {
  return <p className="text-text-muted text-sm max-w-4xl">No holdings yet. Add one above.</p>;
}

// AFTER:
if (!rows || rows.length === 0) {
  return (
    <EmptyState
      title="No holdings yet"
      description="Add your first holding using the form above to start tracking your portfolio."
      className="max-w-4xl"
    />
  );
}
```

---

**Modify `frontend/src/pages/WatchlistPage.jsx`:**

Import `Skeleton` from `../components/ui/Skeleton`. Import `EmptyState` from `../components/ui/EmptyState`. (Table/Th/Td already imported.)

Replace the loading early return (lines 136-138) with skeletons:

```jsx
if (loading) {
  return (
    <div>
      {/* Mobile card skeletons */}
      <div className="sm:hidden space-y-2">
        {[...Array(5)].map((_, i) => (
          <div key={i} className="bg-card border border-border rounded p-3">
            <div className="flex justify-between mb-2">
              <Skeleton className="h-4 w-12" />
              <div className="flex gap-1">
                <Skeleton className="h-4 w-10 rounded-full" />
                <Skeleton className="h-4 w-10 rounded-full" />
              </div>
            </div>
            <div className="flex justify-between">
              <Skeleton className="h-5 w-20" />
              <Skeleton className="h-4 w-12" />
            </div>
          </div>
        ))}
      </div>
      {/* Desktop table skeletons */}
      <div className="hidden sm:block overflow-x-auto">
        <Table className="max-w-4xl">
          <thead>
            <tr>
              <Th>Symbol</Th>
              <Th>Name</Th>
              <Th>Tags</Th>
              <Th align="right">Price (EUR)</Th>
              <Th align="right">24h %</Th>
              <Th align="right">Volume (EUR)</Th>
              <Th align="center"></Th>
            </tr>
          </thead>
          <tbody>
            {[...Array(5)].map((_, i) => (
              <tr key={i} className="border-b border-border/50">
                <Td><Skeleton className="h-4 w-12" /></Td>
                <Td><Skeleton className="h-4 w-20" /></Td>
                <Td><div className="flex gap-1"><Skeleton className="h-4 w-10" /><Skeleton className="h-4 w-10" /></div></Td>
                <Td align="right"><Skeleton className="h-4 w-16" /></Td>
                <Td align="right"><Skeleton className="h-4 w-12" /></Td>
                <Td align="right"><Skeleton className="h-4 w-16" /></Td>
                <Td align="center"><Skeleton className="h-4 w-16" /></Td>
              </tr>
            ))}
          </tbody>
        </Table>
      </div>
    </div>
  );
}
```

Desktop skeleton headers match WatchlistPage's 7 columns exactly. Mobile card skeletons include rounded-full pill placeholders to suggest tag badges.

Replace the empty state text (lines 202-207) with EmptyState component. The two different messages (filtering vs truly empty) each get their own EmptyState:

```jsx
{filteredEntries.length === 0 ? (
  activeTags.length > 0 ? (
    <EmptyState
      title="No entries match the selected tags"
      description="Try removing some tag filters or add coins with those tags."
    />
  ) : (
    <EmptyState
      title="Watchlist is empty"
      description="Add coins using the form above or star them on the Prices tab."
    />
  )
) : (
  /* existing table rendering */
)}
```

---

**Modify `frontend/src/pages/AlertsPage.jsx`:**

Import `Skeleton` from `../components/ui/Skeleton`.

Replace the loading early return (lines 59-61) with card-only skeletons (AlertList is already card-based, same layout on mobile and desktop):

```jsx
if (loading) {
  return (
    <div>
      <h2 className="text-lg font-bold text-text mb-4">Price Alerts</h2>
      <div className="max-w-4xl space-y-2">
        {[...Array(5)].map((_, i) => (
          <div key={i} className="bg-card border border-border rounded p-3 flex flex-col gap-2 sm:flex-row sm:justify-between sm:items-center">
            <div className="flex items-center gap-3">
              <Skeleton className="h-4 w-12" />
              <Skeleton className="h-4 w-20" />
              <Skeleton className="h-5 w-14 rounded-full" />
            </div>
            <Skeleton className="h-8 w-16" />
          </div>
        ))}
      </div>
    </div>
  );
}
```

Card skeleton matches AlertList's existing card structure: flex-col on mobile stacking to flex-row on sm+. Includes placeholders for symbol, price, direction badge, and remove button.

---

**Modify `frontend/src/components/AlertList.jsx`:**

Import `EmptyState` from `./ui/EmptyState`.

Replace the empty state text (lines 6-8) with contextual EmptyState messages. The `title` prop already provides context ("Active alerts" or "Triggered alerts"):

```jsx
if (!alerts || alerts.length === 0) {
  const isTriggered = title.toLowerCase().includes('triggered');
  return (
    <EmptyState
      title={isTriggered ? 'No triggered alerts' : 'No active alerts'}
      description={
        isTriggered
          ? 'Your alerts will appear here when target prices are reached.'
          : 'Set up a price alert using the form above to get notified when prices move.'
      }
      className="max-w-4xl"
    />
  );
}
```

This replaces the generic "No {title}" text with descriptive, contextual messages that differ between the active and triggered alert sections.

#### Verification

```bash
cd frontend && npx vitest run --reporter=verbose
```

All existing tests pass (271+ tests). No regressions from skeleton/empty state integration.

```bash
cd frontend && npx vite build
```

Build succeeds with no errors.

## Success Criteria

- [ ] `Skeleton.jsx` exists in `frontend/src/components/ui/` with `animate-pulse bg-border rounded` and className passthrough
- [ ] `EmptyState.jsx` exists in `frontend/src/components/ui/` with title (required) and description (optional)
- [ ] 3 Skeleton tests pass (animate-pulse class, custom className, prop passthrough)
- [ ] 4 EmptyState tests pass (title renders, description renders, no description paragraph when omitted, custom className)
- [ ] PricesPage shows card skeletons on mobile (`sm:hidden`) and table skeletons on desktop (`hidden sm:block`) when `!prices`
- [ ] PortfolioPage shows card skeletons on mobile and table skeletons on desktop when `loading` is true
- [ ] WatchlistPage shows card skeletons on mobile and table skeletons on desktop when `loading` is true
- [ ] AlertsPage shows card-only skeletons (matching AlertList layout) when `loading` is true
- [ ] PortfolioTable empty state uses `<EmptyState title="No holdings yet" description="..." />`
- [ ] WatchlistPage empty state uses `<EmptyState>` with different messages for filtering vs truly empty
- [ ] AlertList empty state uses `<EmptyState>` with contextual messages for active vs triggered sections
- [ ] All 271+ existing tests still pass
- [ ] `npx vite build` succeeds
